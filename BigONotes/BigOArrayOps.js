// push O(1)
// pop O(1)

// adding to the beginning of an array is more complex bc it needs to reindex everything

// shift O(n)
// unshift O(n)

// concat O(n)
// slice O(n)
// splice O(n)
// sort O(n*logn)
// forEach/map/filter/reduce/etc. O(n)